# Docs Archive

This folder stores one-off prompts, local command notes, pointer diagrams, and older generated artifacts that should not drive current GD1 implementation decisions.

Archived files are kept for recovery and traceability. Do not treat them as source of truth unless a current doc links to them explicitly.
